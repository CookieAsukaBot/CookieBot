module.exports = (bot, config) => {
    // Se unió al servidor
    bot.on('guildMemberAdd', async (member) => {
    });

    // Se fue del servidor
    bot.on("guildMemberRemove", async (member) => {
    });
};
